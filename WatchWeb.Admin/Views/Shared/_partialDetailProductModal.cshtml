@model WatchWeb.Service.Models.Dto.ProductDetailDto
<div class="modal fade" id="myModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">
                    Thông tin chi tiết sản phẩm
                </h5>
            </div>
            <div class="modal-body d-flex">
                <div>
                    <ul class="list-group">
                        <li class="list-group-item">
                            <label asp-for="Id" class="control-label">Mã sản phẩm: @Model.Id</label>
                        </li>
                        <li class="list-group-item">
                            <label asp-for="Name" class="control-label">Tên sản phẩm: @Model.Name</label>
                        </li>
                        <li class="list-group-item">
                            <label asp-for="Status" class="control-label">
                                Trạng thái: @if (Model.Status == 1)
                                {
                                    <span>Hoạt động</span>
                                }
                                else
                                {
                                    <span>Ngưng hoạt động</span>
                                }
                            </label>
                        </li>
                    </ul>
                </div>
                <div style="margin: -11px 24px 24px 24px;">
                    <img id="previewImage" class="img-thumbnail" src="@Model.ImageUrl" asp-append-version="true" style="max-width: 200px; max-height: 200px; margin-top: 10px; display: block;" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary closeModal" data-bs-dismiss="launchModal">Đóng</button>
            </div>
        </div>
    </div>
</div>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
<script>
    $(document).ready(function () {
        $('.closeModal').click(function () {
            $('#myModal').modal('hide');
        });
    });
</script>
